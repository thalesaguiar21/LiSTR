proc main(){
	rational x, y;
	int c;
	write "Digite dois numeros racionais: \n";
	read x y;
	write "Digite um numero inteiro: ";
	read c;
	//conversao implicita de alargamento permitida pela linguagem
	write "( "x ") * (" x ") - (" y ") + " c " = " x * x - y + c "\n";	
}
