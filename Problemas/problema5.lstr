proc mdc(int n, int m, inout int r){
	if(m % n == 0){
		r = n;
	}
	else{
		if(n % m == 0){
			r = m;
		}
		else{
			if(m > n){
				mdc(n, m%n, r);
			}
			else{
				mdc(m, n%m, r);
			}
		}
	}
}

proc main(){
	int a, b, t = 0;
	read a b;
	mdc(a, b, t);
	write t;
}